using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TrackPoint : MonoBehaviour
{
    public int glue = 0;
    public int plastic = 0;
    public int stick = 0;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*private void OnGUI(){
        GUI.Label(new Rect(10,10,100,20), "glue: " + glue);
        GUI.Label(new Rect(10,20,100,20), "plastic: " + plastic);
        GUI.Label(new Rect(10,30,100,20), "stick: " + stick);
    }*/
}
